# Root env example (not committed secrets)
# Run `cp .env.example .env` and fill values for development

# Frontend
VITE_AMAP_KEY=
VITE_API_BASE_URL=http://localhost:3002

# 高德地图后端代理 Key（请在 server 环境或部署环境中设置）
AMAP_KEY=

# Backend defaults
PORT=3002
NODE_ENV=development
DB_PATH=./server/data/panorama_map.db
UPLOAD_DIR=uploads
MAX_FILE_SIZE=52428800
JWT_SECRET=replace_this_with_strong_secret
CORS_ORIGIN=http://localhost:3000
LOG_LEVEL=info
