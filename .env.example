# MyMap Docker 部署环境变量配置示例
# 复制此文件为 .env 并修改实际值

# 服务端口映射（主机端口:容器端口）
MYMAP_PORT=50000

# 安全密钥配置 - ⚠️ 生产环境必须修改这些默认值！
# 密码加密的全局密钥（修改后会导致所有用户密码失效）
PASSWORD_PEPPER=dev-pepper

# Session 密钥（用于会话加密）
SESSION_SECRET=dev-session-secret

# JWT 密钥（如果使用 JWT 认证）
JWT_SECRET=default-secret-key

# 跨域配置（生产环境建议指定具体域名）
# CORS_ORIGIN=https://yourdomain.com

# 其他可选配置
# LOG_LEVEL=info
# MAX_FILE_SIZE=52428800
