# client/.env.example
# copy to client/.env for local development
# NOTE: 项目已迁移为后端代理 AMap 请求，推荐不要在前端存放高德 Key。
# 仅在特殊场景（本地开发、临时调试）下可使用 VITE_AMAP_KEY，但生产环境请把 Key 放在后端环境变量 AMAP_KEY。

VITE_API_BASE_URL=http://localhost:3002
